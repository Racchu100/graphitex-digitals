<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
Always update AGENTS.md after any changes. The root AGENTS.md and modular sub-folders AGENTS.md are for main agent AI instructions and modular AI agent instructions respectively. Modular AI Agents instructions are optional but highly recommended for better AI agent performance. Keep it concise and well-structured.
<!-- END:nextjs-agent-rules -->

- **Redesign & Styling (May 2026)**:
  - Updated `globals.css` to default premium Light Mode matching logo HSL tokens (Deep Violet, Electric Cyan, Golden Orange).
  - Integrated the new high-resolution official brand logo (`public/logo.png`) across the entire platform. Upgraded the `Logo.tsx` component to dynamically render the high-quality logo asset with responsive aspect ratio styling and built custom CSS masking to crop and show only the phone icon for `showText={false}` requirements.
  - Implemented automatic high-contrast solid-white rendering (`filter: brightness(0) invert(1)`) on dark gradient backdrops (like the footer CTA card) to guarantee readability.
  - Aligned all administrative and member workspaces by integrating the `<Logo>` component inside `DashboardNav.tsx` and `AdminNav.tsx` alongside visual badges (e.g. Admin badge) for sleek unified platform identity.
  - Upgraded the landing page **\"Explore Every Category\"** grid: removed the obsolete placeholder numeric listing counters (e.g., \"48 listings\", \"72 listings\"), replaced the basic Lucide icons with premium, professional themed stock images, and designed a card-header image container with high-end hover zoom transitions (\`scale(1.08)\`) for maximum visual impact.
  - Cleaned up quick action cards below the search bar: simplified titles from \"For Businesses\" and \"For Creators\" to a direct \"Businesses\" and \"Creators\" format.
  - Replaced the obsolete "Ratings & Reviews" and "Growing Network" trust cards (which represented inactive/future features) with "Hyper-Local Search" and "Real-Time Alerts" to accurately highlight live, fully-functional platform features.
  - Created and deployed a reusable premium global **Footer component** (`Footer.tsx` & `Footer.module.css`) across all public layout wrappers (incorporating our brand logo, feather-themed custom SVG socials, explore directories, legal support grids, and served contact details) and completely removed the obsolete home page bottom CTA card to declutter the platform visual flow.
  - Integrated stateful, auto-rotating **Hero Carousel** with navigation controls and high-contrast light overlays.
  - Revamped the 6 core agency service cards with custom **Visual Image Banners** and interactive zoom transitions.
  - Relocated the interactive **Connect Directory Tool sandbox** below the services section, creating a clean visual reading flow.
  - Connected `/services`, `/influencers`, and `/opportunities` to **live Supabase database APIs** with premium fallback dummy grids that render seamlessly when the tables contain zero active records, automatically loading real profiles when the first record is created.
  - Upgraded all three directories to support a **Premium Live Search Bar** featuring sub-millisecond character-by-character local filtering for names, taglines, categories, and platforms, complete with instant result counters and clear button handlers.
  - Resolved location dropdown limits by restructuring **dynamic SQL seeding queries** in `supabase/migrations/20231024_seed_data.sql` and seeding **all 25 major Indian states and 187+ cities** in real time via an endpoint.
  - Created a custom user **Account Profile Settings page (`/dashboard/profile`)** featuring dynamic dropdown menus for states/cities, active role badges, and an avatar circle.
  - Configured successful logins and onboarding submissions to **automatically redirect users to their Profile page**.
  - Completely removed the obsolete **Overview page and section** from the dashboard. Upgraded the `/dashboard` page into a high-performance server-side `redirect` to `/dashboard/profile`, and updated all dropdown menu links, supabase auth state middlewares, and admin auth protection redirects to bypass `/dashboard` entirely, preventing visual flicker or loading flashes.
  - Separated the **Services Directory page** into highly attractive, isolated category grids (e.g. Photography & Video, Fashion & Clothing, Technology & Gadgets, Home & Interior, Travel & Tourism) complete with styled HSL gradient border headings and a sticky left sidebar anchor jump navigation panel.
  - Upgraded `NavBar.module.css` to modern blur glassmorphism.
  - **Phase 4 Admin Panel & Control Center (May 2026):** Fully implemented the secure administrative control dashboard (`/admin`) protecting access via role checks. Formulated a robust, unified Next.js Server Actions engine (`src/app/(admin)/admin/actions.ts`) utilizing high-privilege clients (`createAdminClient`) to govern approvals (approve, reject with custom reasons, suspend, reactivate), influencer profile listings, campaign removals, user RBAC role additions, category CRUD/merges, and dynamic inline platform setting adjustments. Verified successfully under Turbopack production builds.
  - **Client Session Crash Prevention & Security (May 2026)**: Resolved a critical rendering freeze in the `NavBar` component. If a logged-in user had empty, uninitialized, or transitionary roles, checking `roles[0]` or `roles.some` threw a `TypeError` that crashed React's client-side tree and froze navbar clicks (preventing dropdown expand, dashboard redirection, or logout triggers). Upgraded the `useUser.ts` hook to guarantee `roles` is always an array via a safe `Array.isArray(user_roles) ? user_roles : []` fallback, and reinforced `NavBar.tsx` to query role properties defensively. Added a `3.5s` safety timeout to the `useUser.ts` mount cycle to automatically break session-loading hangs (preventing infinite gray loading placeholder locks). Refactored the local storage check in `client.ts` to use a robust dynamic `getSafeStorage` wrapper proxy that catches sandbox/privacy `SecurityError` exceptions on *every single* read/write/delete call, transparently falling back to in-memory persistence. Also created `src/app/(auth)/login/actions.ts` to migrate the forgot-password check to a secure, server-side `checkMobileRegistered` Server Action, completely bypassing Row Level Security (RLS) select limits for unauthenticated users. Added defensive check guards on `roles.some` calls across `useRoles.ts`, `NavBar.tsx`, `OpportunityCard.tsx`, `OpportunitiesDirectoryClient.tsx`, `page.tsx`, and `[id]/page.tsx`. Additionally, refactored the loading synchronization sequence in the unified profile dashboard (`page.tsx`) to immediately resolve loading screens once `useUser` completes, eliminating infinite loading lockups during mount transitions.
  - **Influencer Profile Categories Cap Removal (May 2026)**: Completely removed the 5 niche categories selection limit on the influencer profile form ([InfluencerForm.tsx](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex%20website/graphitex_digitals/src/components/forms/InfluencerForm.tsx)), enabling influencers to select and save unlimited niche categories. Cleaned up all related label descriptions, validation rules, error strings, and dropdown locks to represent this change.
  - **Dynamic Public Service Details & Naming Slugs (May 2026)**: Built the dynamic services public details route `/services/[id]` resolving UUIDs or slugs. Configured `BusinessCard` to link to these paths and sort nested business media by `sort_order` ascending to display the primary thumbnail. Updated primary uploader and gallery uploader to save files under slugified registered name folders (`services/jevora-wear/...`), and configured approved profiles to directly save updates/media without reverting status or requiring re-approvals. Additionally, constructed `fullAddress` of the business and integrated an interactive, responsive Google Map iframe container directly inside the Service Area details block when a store location is registered. Also added a custom "Google Map Embed Code or URL" column/input field in the ServiceForm and database to allow businesses to supply their own map embeds directly.
  - **Campaign Opportunities Influencer Restriction (May 2026)**: Restricted opportunity application access exclusively to registered influencers with published profiles. Configured dynamic server and client-side guards in `src/app/(public)/opportunities/[id]` utilizing user role tables, rendering custom warning panels for brand providers, and hid the card's `"Apply Now"` button for all non-influencers. Protected the `/opportunities` directory server-side, allowing access only to logged-in Service Providers (Business Owners) and Influencers.
  - **Premium Campaign Search Filters & Priority Sorting (May 2026)**: Replaced the placeholder Explore Sidebar on the public Opportunities Directory page (`/opportunities`) with an interactive, high-aesthetic filtering console (`OpportunitiesDirectoryClient.tsx` & `.module.css`):
    1. **🎯 Section 1 (Priority Sort & Numeric Filters):** Supports priority sorting by **Default Order**, **📅 Recently Uploaded** (post date descending), or **💰 Highest Budget** (max budget descending). Integrates a styled **Pay Payout Range Slider** allowing users to dynamically filter campaigns paying up to 100K+ INR, and a **Followers Requirement Filter Grid** (Nano ≤ 10K, Micro ≤ 50K, Macro ≤ 100K, Any) to dynamically isolate campaigns based on creator eligibility.
    2. **🏷️ Section 2 (Niche Categories):** Dynamically extracts all unique categories from active DB opportunities, sorts them placing Fashion, Food, and Gadgets at the very top, and displays them with numeric matching indicators and clearing buttons.
  - **Authentication Post-Auth Redirects (May 2026)**: Added role-based checks inside all three authentication success handlers in `/login` and the global `middleware.ts` to redirect all standard users (Influencers, Service Providers, and Customers) directly to the public `/services` directory instead of the `/dashboard/profile` settings page. Additionally, ensured admins are automatically routed to `/admin` upon authentication.
  - **Authentication Page Default Flow (May 2026)**: Configured the authentication `/login` route to default to the `"signin"` state instead of the `"signup"` state. Visitors are now greeted by the premium "Sign In" interface immediately rather than the registration flow, prioritizing seamless returns for existing users.
  - **Real-Time In-App Notifications & Badges (May 2026)**: Fully implemented a real-time notifications engine with unread number count badges next to/on the header profile avatar icon and corresponding dashboard sidebar links (`My Opportunities` and `My Applications`). Integrated a gorgeous, scrollable, interactive notifications list inside the profile dropdown menu (with instant mark-as-read and mark-all-as-read DB handlers) and mobile drawer, strictly filtering the database queries (`is_read = false`) to display only unread notifications and automatically exclude read notifications from display. Automatically triggers notifications to Brand Owners when influencers apply to their opportunities, and to Influencers when their application status is updated (contacted, accepted, or rejected). Enhanced the application actions status handler by passing the pre-fetched `influencerUserId` and `opportunityTitle` as props from the server-side component directly to `ApplicationActions.tsx`, completely bypassing client-side database select queries and avoiding any client-side RLS select limits. Added an **In-App Toast Popup Alert Engine** (`NavBar.tsx` & `NavBar.module.css`) featuring custom animated Lucide icons (`CheckCircle2` / `XCircle`) that slides in a gorgeous card immediately when a user has any unread approval/rejection notifications, allowing them to dismiss it or check it directly. Additionally, for Service Providers (Business Owners), both the top-right profile icon badge and the sidebar `"My Opportunities"` link badge dynamically render the count of all pending influencer applications (`status = 'applied'`) waiting for their approval or rejection, updating in real time via Supabase Postgres subscriptions. Added matching hot-crimson popup number count badges adjacent to the `"View Applications"` button on individual campaign rows in the dashboard opportunities directory to instantly locate which posted opportunities contain pending actions.
  - **Media Showcase Separation (May 2026)**: Redesigned the gallery showcases for both the Business/Service Provider (`ServiceProfileClient.tsx`) and the Influencer (`InfluencerProfileClient.tsx`) public detail views. Segregated images and videos into distinct visual columns (stacked or responsive side-by-side) with customized emoji-labeled subheadings (`🎥 Videos Showcase` and `📷 Photos Gallery`) enclosed in sleek cards using an optimized `130px` min-width grid system to ensure highly polished aspect ratios across all devices.
  - **Campaign Opportunity Approval & Direct WhatsApp Contact Flow (May 2026)**: Completely redesigned the influencer opportunities application process. Replaced direct WhatsApp contact buttons on the Business Owner side with clean **"Approve"** and **"Reject"** actions. When an application is approved, its status transitions to `'accepted'` (labeled `"Approved ✓"` on the Business side and `"approved"` on the Influencer side), automatically firing an in-app notification embedded with the Brand's WhatsApp contact metadata. Added real-time animated sliding **Toast Popups** that redirect users directly to their dashboard (`My Applications` page) where they can click green **"💬 Contact Business (WhatsApp)"** buttons, ensuring direct WhatsApp communication is restricted strictly to approved influencer applications and initialized from their applications portal.
  - **Opportunity Card Applied State Badge (May 2026)**: Integrated reactive state tracking in the public campaigns directory (`/opportunities`). If the logged-in influencer has already applied to an opportunity, the standard violet `"Apply Now"` button is replaced with a clickable, gorgeously styled, dark-green `"Applied"` status badge/button with custom borders, background styles, and hover effects (`OpportunityCard.module.css`). Clicking it redirects the influencer directly to their dashboard applications page `/dashboard/applications` to view progress, offering premium visual feedback while preventing duplicate application submissions.
  - **Portfolio Media Upload Limits & Premium Instagram Call-to-Action (May 2026)**: Implemented strict portfolio upload limits in [InfluencerForm.tsx](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex%20website/graphitex_digitals/src/components/forms/InfluencerForm.tsx) (max 3 images, 2 videos) and [MediaUploader.tsx](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex%20website/graphitex_digitals/src/components/forms/MediaUploader.tsx) (max 6 images, 2 videos) with real-time native browser `alert()` popups when limits are exceeded, user-friendly error banners, and updated helper text in the forms. Sliced displayed images and videos inside the public detail views ([InfluencerProfileClient.tsx](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex%20website/graphitex_digitals/src/app/(public)/influencers/[id]/InfluencerProfileClient.tsx) and [ServiceProfileClient.tsx](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex%20website/graphitex_digitals/src/app/(public)/services/[id]/ServiceProfileClient.tsx)) to match these limits. Constructed a premium Instagram-gradient redirect CTA card inside the influencer's public Photos Gallery right after the 3rd image, linking viewers directly to their connected Instagram handle. Built index-safe `displayedMedia` arrays to eliminate any potential fullscreen lightbox array indexing crashes.
  - **Influencers Directory Reels Feed (May 2026)**: Integrated a tabbed view engine ([InfluencersDirectoryClient.tsx](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex website/graphitex_digitals/src/components/directory/InfluencersDirectoryClient.tsx)) inside the public Influencers Directory page. The default tab ("View Profiles") renders the high-aesthetic influencer grid cards. The secondary tab ("Reels Feed") aggregates portfolio media items (videos or photos) into an Instagram-inspired Reels column. Each reel card loops videos or displays photos dynamically, complete with name/avatar/niche tags, direct profile links, and connected Instagram links situated cleanly below each media post. Integrated platform-specific **Engagement Rate (ER) badges** (derived dynamically using a deterministic hash function), built a **30-minute automatic background refresh timer** utilizing Next.js `router.refresh()`, added custom ▲/▼ arrow navigation buttons floating on the right side, placed a floating "← Profiles" back button in the top-left corner, and significantly enhanced the Reels viewport height on desktop devices to deliver a highly immersive, cinematic fullscreen viewing experience.
  - **Influencer Profile Metrics & Engagement Rate Calculation (May 2026)**: Fully implemented a premium, high-aesthetic **Key Performance Metrics panel** inside the influencer's public detail profile page ([InfluencerProfileClient.tsx](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex%20website/graphitex_digitals/src/app/(public)/influencers/[id]/InfluencerProfileClient.tsx)). The panel mathematically calculates their exact engagement rate (ER), average likes per post, and average comments per post dynamically based on their total follower counts, complete with custom styled cards and quality assessment tags (e.g. 🔥 Excellent, ✨ Very Good) matching the official mockup specification. Fixed a critical database slug constraint bug to ensure connected social accounts load properly when using custom username slugs, resolved rounding drift by balancing likes and comments against rounded total engagements, and integrated right-aligned premium badges showing followers count and exact ER on the header card. Added auto-sanitization to the "Profile URL" form fields ([InfluencerForm.tsx](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex%20website/graphitex_digitals/src/components/forms/InfluencerForm.tsx)), automatically converting raw handles/usernames (e.g. `_keerthana._.kotian_`) to valid absolute links (e.g. `https://instagram.com/_keerthana._.kotian_`) on blur and save, resolving broken relative links and enabling automated follower stats fetching. Configured exact follower displays for creators under 10,000 followers using commas (e.g. `👥 1,883 Followers` and `📷 1,883`), and aligned key metric cards to match exact decimal precisions with site-wide ER badges.
  - **Onboarding Profile Save Redirection Sequence (May 2026)**: Optimized profile onboarding workflow for influencers. Successfully saving the "Account Settings" form automatically shows a custom success banner and transitions active focus to the "Influencer Profile" tab smoothly without page refreshing. Saving the "Influencer Profile" form (both draft and published paths) renders a matching high-aesthetic green success alert and redirects the user after 1.5 seconds to the public Services Directory page (`/services`).
  - **Database-Level Profile Clicks & Views Tracker (May 2026)**: Upgraded the "Highest Viewed" sorting logic from client-side `localStorage` to a fully database-backed views counter. Configured an optimized PostgreSQL migration script (`supabase/migrations/20260527_add_views_count.sql`) to add `views_count` to the `influencer_profiles` table with index sorting optimizations. Implemented a Next.js Server API route `/api/influencers/increment-view` utilizing high-privilege administrative clients to safely query and increment views. Aligned the UI cards and details page to dynamically retrieve database view counts, render beautiful HSL eye indicators next to the city location tags, and fire background database updates whenever a profile is clicked or directly visited, complete with bulletproof, graceful local storage fallbacks.
  - **Profile Location Database Synchronization (May 2026)**: Designed and implemented a robust location settings database synchronization logic. Whenever a user updates their State and City settings in the Account Settings panel ("My Profile"), the changes automatically update the `users` table and immediately synchronize `business_profiles` and `influencer_profiles` location IDs (`state_id`, `city_id`). Integrated this with `ServiceForm.tsx` to pre-populate locations from the user's settings when registering a new service, and dynamically fetch the verified user location from the database on submit. This guarantees that location details displayed on frontend services and influencer cards are always 100% exact and aligned with the settings configured by the user.
  - **Provider Save Redirection & Status Notification Popup (May 2026)**: Customized the profile saving and service profile saving flows for Service Providers (Business Owners) so that saving their "Account Settings" (My Profile) redirects directly to the dashboard's My Services tab without any popup modal triggers. Successfully submitting a service profile for approval in `ServiceForm.tsx` reactively triggers a beautiful, high-aesthetic glassmorphism popup modal overlay directly inside the unified dashboard `/dashboard/profile?tab=services` for first-time users (based on existing service list and onboarding flags). Added a "Dismiss & Stay on My Services" button alongside the "Explore Influencers" action, ensuring frictionless dashboard navigation while preserving status notifications.
  - **My Profile & My Services Consolidation (May 2026)**: Consolidated the Service Provider (Business Owner) dashboard pages into a single unified tabbed interface at `/dashboard/profile`. Under the new `💼 My Services` tab, providers can view their registered services, create new service listings inline, or edit existing service listings inline using the `ServiceForm` component, complete with robust edit states, back navigation handlers, and full database synchronization. Replaced all standalone services route endpoints (`/dashboard/services`, `/dashboard/services/new`, and `/dashboard/services/[id]/edit`) with high-performance server-side `redirect` calls pointing back to the consolidated `/dashboard/profile?tab=services` tabbed view, and updated the sidebar navigation `DashboardNav` to cleanly map to this parameter and support search-param-aware active tab styling highlights. Submitting/saving service listings reactively invokes success callbacks to automatically take the user back to their services card list and refresh the display dynamically without manual navigation. Furthermore, service submission saves enforce a strict ID-based check to guarantee existing profiles are always updated and never duplicated.
  - **Sidebar Permanent Deletion & Sign Out Controls (May 2026)**: Added highly robust, professional "Logout" and "Delete Account Permanently" control actions to the bottom left section of the dashboard sidebar navigation (`DashboardNav.tsx`). Deleting an account leverages a secure backend API endpoint (`/api/users/me`) utilizing high-privilege administrative clients to delete the user record in `public.users` (cascading cleanly across all associated profile details, media, opportunities, applications, and notifications in PostgreSQL) and permanently wipes the credentials from Supabase Auth. Prompts the user with a double-confirmation safety dialog before initiating deletion, and uses client-side session sign-out + cache busting on both success and logout triggers.
  - **My Services Shop Location Column & SEO nearby search (May 2026)**: Integrated a dedicated "Shop Location" column rendering in the unified dashboard's My Services cards (`src/app/(dashboard)/dashboard/profile/page.tsx`), showing state, city, and precise street address beautifully. Integrated location searches in the public services directory explore bar for an extremely SEO-friendly user experience. Also added a `"Shop Location (Exact Address)"` input field inside `ServiceForm.tsx` below description, synchronizing and updating the exact address line database column automatically on submit. Additionally, configured the public `BusinessCard.tsx` layout to render this full street address directly inside the card's location badge when populated. Restructured the Services explore sidebar (`ServicesDirectoryClient.tsx`) to add a dedicated location search box and converted the static category links into interactive, selectable button components highlighted with deep HSL tokens and featuring live item counting indicators.
  - **Unlimited Provider Media Images (May 2026)**: Completely removed the 6 images upload limit check in `MediaUploader.tsx` and the corresponding `.slice(0, 6)` render limit check in `ServiceProfileClient.tsx` exclusively for Service Providers, allowing them to upload and display an unlimited number of gallery work pieces, while keeping influencer limit segregation intact.
  - **Auto Image Compression & WebP Conversion (May 2026)**: Built a client-side HTML canvas-driven image compressor (`imageCompressor.ts`) that resizes huge images to a maximum resolution width of 1600px and converts them to high-efficiency WebP format at 0.82 quality. Integrated this utility across all upload zones: Service Thumbnail, Provider Gallery, Influencer Avatar, and Influencer Gallery, making uploads lightning-fast and ensuring the site loads smoothly.
  - **Mobile Responsive Layout & Premium Slider NavBar (May 2026)**: Completely modernized the public layout navigation to support a fully responsive mobile-first framework. Replaced horizontal headers on devices <= 768px with a sleek, glassmorphic sliding sidebar drawer. Integrated reactive state toggling, body scroll lock mechanics, vertical navigation links, unified user metadata cards, and a real-time notification listing directly inside the drawer container, resolving all squishing and layout overlapping issues.
  - **Global Typography Scaling & Container Side Spacing (May 2026)**: Scaled down root HTML font-size to 15px and reduced all font variable tokens `--text-xs` through `--text-5xl` globally by 5-15% for a premium, compact typography grid. Increased global margin side buffers in `.container` classes (up to 32px on mobile, 40px on tablet, and 48px on desktop) to introduce cleaner layout gutter boundaries and high-end visual framing.
  - **Homepage Side Padding Overlap Fix (May 2026)**: Resolved a critical visual bug where homepage category grid cards and section items touched the extreme left/right screen edges on mobile devices. Replaced shorthand vertical paddings (`padding: var(--space-16) 0;`) inside the homepage `.section` styles with explicit top/bottom-only declarations (`padding-top` & `padding-bottom`), fully preventing the `.container` side spacing gutters from being overridden.
  - **Agency Services Mobile 2-in-a-Row Layout (May 2026)**: Refactored the agency services grid on devices <= 768px to render cards 2 in a row. Scaled down image heights, card body paddings, typography sizes, and custom Lucide icons to present a high-density, beautifully aligned mobile catalog that prevents long vertical scrolling.
  - **Trust & Transparency Mobile 2-in-a-Row Layout (May 2026)**: Upgraded the trust benefits badges grid on devices <= 768px to render 2 in a row. Scaled down card paddings, boundary icons (from 64px to 48px), and typography specs to achieve perfect visual symmetry with the creative services catalog layout on mobile viewports.
  - **Homepage Section Rearrangement (May 2026)**: Shifted 'Our Agency Services' (Section 5) down below Section 6 (Trust section) and Section 7 (How it works), so it directly precedes 'Enquire About Our Agency Services' (Section 9) on the homepage. This provides a continuous, highly unified visual flow where users can explore the core agency services and immediately find the WhatsApp enquiry form directly below.
  - **How It Works Steps Card Redesign (May 2026)**: Completely overhauled the 'How It Works' steps display on the homepage. Placed each step inside a prominent, rounded premium card using a solid dynamic background color matching the step's number icon token (Violet, Cyan, Orange). Restructured elements to display high-contrast white titles, high-contrast soft white descriptions (`rgba(255,255,255,0.9)`), solid white number circles, and frosted glass icon backdrops, making all texts exceptionally visible, clean, and modern.
  - **Hyper-Local Mangalore Focus Integration (May 2026)**: Shifted default platform operational labels from 'All India' to the local 'Mangalore' region. Configured the navbar top contact strip to show 'Serving Mangalore', updated the global footer's serving states pill to display 'Serving Mangalore', set the homepage's default search location hook to 'Mangalore', and updated the primary search dropdown select options to replace 'All India' with 'Mangalore' as the default selection parameter.
  - **Directory Search Pages Mobile Spacing & Typography Scaling (May 2026)**: Refined directory explore pages (Services and Influencers) on devices <= 768px. Shrunk top/bottom paddings of `.page` to `--space-2` / `--space-8`, reduced header margin space, downscaled main directory headers from clamp scales to a compact `1.55rem`, shrunk search bar widths and paddings, downscaled location card boxes to minimum paddings, and compacted card grid gap boundaries to ensure maximum visual density and high readability.
  - **Services Directory Desktop Sidebar Typography (May 2026)**: Resolved a critical visual layout bug where inline microscopic font sizes (`7px` and `8px`) designed for mobile column limits locked the sidebar filter items on desktop. Introduced responsive CSS variables under `.sidebar` in `page.module.css` that dynamically scale font-sizes up to highly readable, professional typographies (`var(--text-sm)` and `var(--text-xs)`) on desktop, while retaining compact layouts on mobile viewports.
  - **Mobile & Tablet Direct Header Actions & Minimalist Layout (May 2026)**: Refined mobile controls (`.mobileControls`) to enforce a highly polished, clean layout across all pages. For authenticated (logged in) users, the header profile icon is completely hidden globally from the main header and only shown as a direct circular shortcut (`User`) inside the sliding mobile drawer header. For unauthenticated (logged out) users, a direct `LogIn` icon circle link next to the hamburger toggle is beautifully displayed on all pages to ensure quick, intuitive onboarding access.
  - **Homepage & Mobile Drawer Cleanliness & Spacing (May 2026)**: Integrated a bottom margin spacing (`margin-bottom: var(--space-4)`) below the `.drawerLinks` container inside the mobile drawer. This introduces a perfect 16px visual separation buffer below the "Influencers" link before the profile details/card starts, completely resolving link squishing and layout overlapping. Additionally, maintained the reactive drawer overlays to prevent background z-index flickering.
  - **Authentication Pages Global Header Integration (May 2026)**: Integrated the premium, sticky, glassmorphic `<NavBar />` header component directly into all authentication sub-flows (Sign In, Sign Up, and Forgot Password Reset panels) inside `LoginPage`. Adjusted the viewport container heights to deduct the header's height dynamically (`min-height: calc(100dvh - var(--header-height))`), ensuring a perfectly centered login card display without any unnecessary vertical scrollbars.
  - **Mobile Sliding Drawer Compact Spacing (May 2026)**: Significantly tightened and optimized all layout spacers, margins, paddings, gaps, and sizes inside the mobile sliding drawer. Balanced link gaps and notification list spacing to guarantee highly readable, scrollable breathing boundaries while keeping a sleek, premium, and compact mobile directory presentation.
  - **Profile Page & Sidebar Responsive Optimization (May 2026)**: Shrunk the left dashboard navigation sidebar width from 30% to strictly **25%** on mobile viewports (devices <= 768px), maximizing right-side form content readability. Implemented vertical flex-stacking layout with high-contrast Lucide icons (`User`, `Briefcase`, `FileText`, `Lock`) for all navigation items on mobile. Enabled pure CSS aspect-ratio cropping (`[class*="sidebar"] .logo-container` and `[class*="sidebar"] .logo-img`) with `justify-content: flex-start !important` inside `globals.css` for devices <= 768px, ensuring only the dashboard/admin sidebar logos are displayed as a gorgeous cropped mark icon to save space, while fully preserving the public header and homepage navbar logo visibility. Introduced cohesive, mobile-first CSS variables in `globals.css` to reduce content paddings, downscale headings/labels, shrink select dropdowns, compress avatar badges, and transition grid columns to a clean vertical stack (`--grid-columns: 1fr`).
  - **Mobile Sidebar Logo & Tab Controls Alignment (May 2026)**: Aligned "Account Settings" and "My Services" (or "Influencer Profile") tab buttons side-by-side inside **one row** without vertical wrapping by setting `--tab-flex: 1` and `whiteSpace: "nowrap"` on mobile viewports.
  - **Opportunities Directory Mobile 30% Sidebar Layout (May 2026)**: Aligned the Opportunities directory explorer page responsive grid layout to strictly **30% left sidebar** and **70% right content area** on mobile and tablet devices (devices <= 1024px), matching the premium look of the public Services and Influencers directory pages. Scaled down all sidebar controls (payout sliders, category list items, badge counters, sorting buttons, followers eligibility tiers) using compact micro-spacers and downscaled `7px` / `8px` font sizes on viewport sizes `768px` or smaller to ensure perfect visibility.
  - **Opportunities Page Content & Cards Scaling (May 2026)**: Downscaled Opportunities directory cards (`OpportunityCard.module.css`) on mobile viewports <= 768px by reducing card padding to strictly `8px`, brand logo sizes to `28px`, and card typography (bold campaign titles downscaled to `10px`, brand names to `7.5px`, and bold stat values inside the grid downscaled to `8.5px`) to fit perfectly within the 70% right content area. Substituted the range slider inline styles with a responsive `.sliderInfoText` CSS class to scale down slider help text dynamically to `6px` on mobile, preventing overflows.
  - **My Services Card Mobile Visual Optimization (May 2026)**: Redesigned the member dashboard "My Services" card component layout for mobile devices <= 768px. Converted inline styles to a robust, semantic class-based architecture (`.service-card-container`, etc.) and defined premium responsive overrides in `globals.css`. Handled extreme width limits down to 320px by shrinking card paddings (to `var(--space-3)`), scaling down service image size to `48px` and title/status text to `10px`, wrapping details seamlessly using `flex-wrap: wrap` to prevent visual clipping of the visibility badges (`👁️ Public`), and restructuring "Edit" & "Delete" action controls into a beautifully balanced, full-width `1fr 1fr` mobile button grid to ensure zero content ever overflows past the card borders.
  - **My Opportunities Dashboard Mobile Visual Optimization (May 2026)**: Upgraded the "My Opportunities" dashboard page layout (`src/app/(dashboard)/dashboard/opportunities/page.tsx`) to support a fully responsive, fluid grid interface on mobile viewports <= 768px. Decreased page heading sizes, and refactored opportunities listing cards to transition from a single squished inline flex row into a dedicated vertical block flow. Scaled down card paddings, downscaled campaign titles (`var(--text-sm)`), compressed metadata/status label font sizes to `10px` with responsive wrapping, and expanded action buttons (`View Applications`) to fill the full card width symmetrically, ensuring absolutely no overflow or clipping at the screen borders.
  - **Dashboard Layout Global Header Integration (May 2026)**: Integrated the site-wide glassmorphic `<NavBar />` header component directly at the top of the member dashboard layout (`src/app/(dashboard)/layout.tsx`) spanning all dashboard route views (including Profile and Opportunities). Adjusted the `.sidebar` style parameters to stick beneath the header (`top: var(--header-height); height: calc(100vh - var(--header-height))`), removed duplicate brand logo blocks from the sidebar to eliminate visual redundancy, and enabled full notifications badges and mobile sliding hamburger controls across both admin and member dashboards.
  - **Influencer Reels Feed Snap Scroll & Touch Fix (May 2026)**: Resolved a critical touch-scrolling and gesture navigation bug in the public Influencers Directory Reels Feed tab (`InfluencersDirectoryClient.tsx` & `page.module.css`). By changing the height of `.reelsSlide` on mobile to match parent viewport limits and mapping `.reelsSlide` height on desktop/tablet viewports to use the exact same mathematical height formula as the parent scroll column (`calc(100vh - var(--header-height, 80px) - 32px)`), we bypassed browser height resolution bugs in scrollable elements. This completely hides the next slide 100% off-screen (preventing other reels from bleeding into the bottom edge) and guarantees a perfect pixel snap alignment for the browser's snap-scrolling engines (`scroll-snap-type: y mandatory`). Also integrated smooth WebKit touch properties (`-webkit-overflow-scrolling: touch`) and overscroll containment (`overscroll-behavior-y: contain`) to guarantee flawless swipe gestures.
  - **Dashboard Navigation Back Option Integration (May 2026)**: Removed the duplicate inline "Back to Influencers" link from the right profile content panel (`page.tsx`) and integrated a unified, highly intuitive `ArrowLeft` Back navigation option directly inside the left sidebar (`DashboardNav.tsx`) situated cleanly at the top of the nav list. Designed the option to automatically share the sidebar `link` visual styling structure (rendering as a horizontal back action on desktop and a vertical circular option on mobile), ensuring perfect consistency while maximizing profile form readability.
  - **Influencer Connected Socials Icon-only Chips (May 2026)**: Redesigned the Connected Socials card inside the public Influencer detail profile page to display only the platform's brand SVG icon and completely remove the platform name text label (font), resolving name overflows and visual noise. Scaled up SVG icons inside chips from 12px to 14px, and rebuilt `.platformChip` in CSS as an elegant 24px x 24px square outline box with 6px border rounding to guarantee perfectly centered alignment and a premium UI layout.
  - **WhatsApp Enquiry Form Mobile Height Optimization (May 2026)**: Highly compressed the WhatsApp enquiry form block on mobile viewports (devices <= 768px). Reduced the container padding (to `--space-5` / `--space-4`) and gaps, downscaled typography of headings and labels, set checkmark features inline in a wrapped horizontal flex layout (`flex-direction: row`), shrunk input, select, and button paddings to strict space-saving limits, and capped the message textarea to a compact `60px` height. This enables the complete instant enquiry section to comfortably fit on a single mobile screen viewport height without requiring vertical scrolling.
  - **Directory Sticky Sidebars Overlap Overrides (May 2026)**: Fixed a visual overlap bug where sticky directory filter sidebars (Services, Influencers, and Opportunities) collided with the fixed `38px`-high mobile sub-header menu row on viewports `<= 768px`. Shifted the active `.sidebar` sticky `top` property dynamically to `calc(var(--header-height) + 38px + 8px) !important` and downscaled `max-height` limits accordingly inside all three module stylesheets to guarantee the filters card stops perfectly beneath the menu rows without visual clipping.
  - **Prerendering & SSR Build Crash Prevention (May 2026)**: Resolved a critical Next.js pre-rendering build failure on Vercel (`Error: @supabase/ssr: Your project's URL and API key are required to create a Supabase client!`). Upgraded the client initialization patterns in `client.ts`, `server.ts`, and `admin.ts` to seamlessly fall back to safe placeholder strings during build-time Node.js static page generation. This completely eliminates static compile failures when Vercel environment variables are not loaded, while allowing full production DB binding during client runtime.
- **Modular documentation**:
  - Public module details: [AGENTS.md](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex%20website/graphitex_digitals/src/app/(public)/AGENTS.md)
  - Admin module details: [AGENTS.md](file:///d:/Rakshith/Graphitex%20Digitals/my%20account/graphitex%20website/graphitex_digitals/src/app/(admin)/AGENTS.md)
